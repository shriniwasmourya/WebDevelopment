<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Insta page</title>
    <style>
        body{
            background-color: aquamarine;
        }
        button{
            height: 30px;
            width: 80px;
            background-color: blue;
            border-radius: 3rem;
            cursor: pointer;
            color: white;
            text-decoration: none;
            margin: 1rem;
        }

        span{
            font-size: 1.5rem;
            padding: 1.2rem;
        }

        img{
            height: 20%;
            width: 30%;
            object-fit: cover;
            border-radius: 6px;
        }
    </style>
</head>
<body>
    <h1>This page belong to @ <%= username %></h1>

    <button>Follow</button>
    <button>Message</button>
    <br>

    <span>Followers : <%= data.followers %></span> 
    <span>Following : <%= data.following %></span>

    <hr>

   <div class="post">
    <% for(let post of data.posts) { %>
        <br>
        <img src="<%= post.image %>" alt="<%= username %> image">
        <br>
        <span>Likes : <%= post.likes %></span><span>Comments : <%= post.comments %></span>
    <% } %>
   </div>
</body>
</html>